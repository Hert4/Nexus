apiVersion: v1
kind: Service
metadata:
  name: langfuse-postgres
  namespace: nexus-ai
  labels:
    app: langfuse-postgres
spec:
  selector:
    app: langfuse-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
