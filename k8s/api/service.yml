apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: nexus-ai
  labels:
    app: nexus-api
spec:
  selector:
    app: nexus-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
