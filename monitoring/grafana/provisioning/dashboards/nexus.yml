# Grafana dashboard provisioning — load dashboards từ /var/lib/grafana/dashboards/
apiVersion: 1

providers:
  - name: nexus-ai
    orgId: 1
    folder: "Nexus AI"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
